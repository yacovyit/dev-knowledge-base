
---

## `tips/daily-tips.md`
```markdown
Always write meaningful commit messages.  
Use `docker system prune -af` to clean disk space.  
Use `git stash` to save changes temporarily.  
In PostgreSQL, use `EXPLAIN ANALYZE` to debug query performance.  
Use `Ctrl + R` in bash to reverse-search commands.  
```
