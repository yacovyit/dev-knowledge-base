## `docker/basics.md`
```markdown
# Docker Basics

```bash
docker ps           # running containers
docker images       # list images
docker run -it alpine sh
docker exec -it <id> bash
docker logs -f <id>
```

